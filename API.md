# API

Especificação viva está no [README](README.md) (rotas, Cognito, curl de lab).

Endpoints:

- `POST /solicitar-exclusao-cliente` (JWT)
- `GET /status-exclusao/{cliente_id}` (JWT)
- `POST /confirmar-pagamento` (JWT)
- `GET /health` (público)
